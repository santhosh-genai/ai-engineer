import numpy as np

class TestReport:
    execution_times = np.array([])

class TestReport:

    def average_time():
        mean = np.mean(TestReport.execution_times)
        return mean

    def max_time():
        maximum = np.max(TestReport.execution_times)
        return maximum
    
    def slow_test(threshold)
    